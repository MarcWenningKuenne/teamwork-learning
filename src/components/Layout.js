import React from "react"

export default function Layout({ children }) {
    return(
        <div>
            {/* app bar */}

            {/* side drawer */}

            <div>
                {children}
            </div>
        </div>
    )
}